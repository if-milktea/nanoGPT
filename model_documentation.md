# nanoGPT モデル構造の解説

## 概要
このドキュメントではmodel.pyに実装されているnanoGPTモデルの構造と各コンポーネントの役割について説明します。

## クラスと関数の説明

### LayerNorm クラス
- **目的**: レイヤー正規化を実行するクラス
- **特徴**: 
  - オプションでバイアスパラメータを含むことが可能
  - PyTorchの標準LayerNormを拡張し、バイアスなしのオプションを追加

### CausalSelfAttention クラス
- **目的**: トランスフォーマーの自己注意機構を実装
- **主要機能**:
  - マルチヘッドアテンションの計算
  - Flash Attentionの自動適用（PyTorch 2.0以上）
  - 因果的マスキング（未来の情報を見ないように制限）
- **重要なメソッド**:
  - `forward`: 入力シーケンスに対して自己注意を計算

### MLP クラス
- **目的**: 多層パーセプトロンを実装
- **構造**:
  - 入力層 → 4倍の中間層 → GELU活性化 → 出力層
  - Dropoutによる正則化

### Block クラス
- **目的**: トランスフォーマーブロックの実装
- **構成要素**:
  - レイヤー正規化（2層）
  - 自己注意機構
  - MLP層
- **特徴**: 残差接続を使用

### GPTConfig データクラス
- **目的**: モデルの設定パラメータを管理
- **主要パラメータ**:
  - `block_size`: 最大シーケンス長
  - `vocab_size`: 語彙サイズ
  - `n_layer`: トランスフォーマーブロック数
  - `n_head`: アテンションヘッド数
  - `n_embd`: 埋め込み次元数
  - `dropout`: ドロップアウト率
  - `bias`: バイアスの使用有無

### GPT クラス
メインのモデルクラス

#### 初期化関数 (`__init__`)
- トランスフォーマー層の構築
- 重み初期化
- パラメータ数の計算と報告

#### 順伝播 (`forward`)
- **入力**: トークンインデックス、（オプション）目標値
- **処理**:
  - トークン埋め込み
  - 位置埋め込み
  - トランスフォーマーブロックの適用
  - 言語モデルヘッドによる予測

#### 生成関数 (`generate`)
- **目的**: 自動テキスト生成
- **特徴**:
  - 温度によるサンプリング制御
  - top-kサンプリングオプション
  - 最大トークン数指定

#### ユーティリティ関数

##### `get_num_params`
- モデルのパラメータ数を計算
- 埋め込み層の除外オプション

##### `crop_block_size`
- コンテキストウィンドウサイズの調整
- 事前学習モデルのブロックサイズ変更に使用

##### `from_pretrained`
- 事前学習済みGPT-2モデルの読み込み
- モデルサイズの自動設定
- 重みの変換と転置処理

##### `configure_optimizers`
- オプティマイザの設定
- 重み減衰の適用制御
- fusedオプティマイザの自動選択

##### `estimate_mfu`
- モデルのFLOPS使用率推定
- A100 GPUのピークパフォーマンスとの比較

## パフォーマンスと最適化
- Flash Attentionの自動活用
- 重み共有による効率化
- fusedオプティマイザの利用
- 推論時の最適化（最終位置のみの計算）

## 参考情報
- OpenAI GPT-2実装をベース
- HuggingFace Transformersライブラリとの互換性
- PyTorch 2.0以上での最適化機能の活用
